# Python sources are formatted with Ruff and stored with one canonical newline.
*.py text eol=lf

# Golden fixtures record exported bytes. Git must never rewrite their line
# endings, or the comparison would depend on the checkout platform.
tests/golden/** -text
tests/golden/*.csv whitespace=cr-at-eol
